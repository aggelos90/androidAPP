
public class class4 implements Runnable {

	public void run() {
		/*
		 * i = 1; int snaplen = 64 * 1024; // Capture all packets, no trucation
		 * int flags = Pcap.MODE_PROMISCUOUS; // capture all packets int timeout
		 * = 10 * 1000; // 10 seconds in millis PcapIf first =
		 * netInterfaces.get(0); Pcap pcap = Pcap.openLive(first.getName(),
		 * snaplen, flags, timeout, errbuffer);
		 * 
		 * if (pcap == null) {
		 * System.err.printf("Error while opening device for capture: " +
		 * errbuffer.toString()); return; }
		 */
	}
}
